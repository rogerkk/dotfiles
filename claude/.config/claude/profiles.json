{
  "default": {
    "mcpServers": {
      "puppeteer": {
        "command": "docker",
        "args": ["run", "-i", "--rm", "--init", "--network=host", "-e", "DOCKER_CONTAINER=true", "mcp/puppeteer"]
      }
    }
  }
}
